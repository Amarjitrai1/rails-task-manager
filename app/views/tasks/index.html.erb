<h1> My Tasks </h1>

<% @tasks.each do |task| %>
<%= link_to task.title, task_path(task) %><br><%= link_to "Delete", task_path(task), method: :delete, data: {confirm: "Fuck it"} %><br>
<% end %>

<%= link_to "Add a task", tasks_new_path %>
